const express=require('express');const cors=require('cors');const bodyParser=require('body-parser');const path=require('path');const api=require('./routes/api');const {PORT}=require('./config/constants');const error=require('./middleware/error');
const app=express();app.use(cors());app.use(bodyParser.json({limit:'2mb'}));app.use(express.urlencoded({extended:true}));app.use('/api',api);app.use(express.static(path.join(__dirname,'../frontend')));app.use((req,res)=>res.sendFile(path.join(__dirname,'../frontend/index.html')));app.use(error);app.listen(PORT,()=>console.log(`Usage Billing SaaS listening on http://localhost:${PORT}`));module.exports=app;
